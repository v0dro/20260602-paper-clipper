# Cloudflare named-tunnel config for Paper Clipper.
#
# Fill in the two placeholders after running (see README.md):
#   cloudflared tunnel login
#   cloudflared tunnel create paperclipper        -> prints <TUNNEL_UUID>, writes ~/.cloudflared/<UUID>.json
#   cloudflared tunnel route dns paperclipper clipper.<yourdomain>

tunnel: <TUNNEL_UUID>
credentials-file: /home/user/.cloudflared/<TUNNEL_UUID>.json

ingress:
  - hostname: clipper.<yourdomain>
    service: http://localhost:8000
  - service: http_status:404
